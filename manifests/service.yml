apiVersion: v1
kind: Service
metadata:
    name: "kubernetsapp"
    labels:
        app: "kubernetsapp"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubernetsapp"