apiVersion: v1
kind: Service
metadata:
    name: "kubernetsaks-f457"
    labels:
        app: "kubernetsaks-f457"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubernetsaks-f457"